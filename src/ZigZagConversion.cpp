class Solution {
public:
    string convert(string s, int numRows) {
        string
        int numCols = (s.size() + numRows - 1)/ numRows;
        char a[numRows][numCols];
        int i = 0;
        while(i < s.size()){
            if(col % 2 == 0){

            }
        }
    }
};
